remove password unique(why did you even do this ?), provider_user_id unq 
remove index on password if it got created .


to be added
add cmop unq to provider and provider id ?
check tokens length
don't create any indexes on provider stuiff for now , if yes then remove 
check if verified at needs onupdate